export { CardGroup, CardGroupRoot } from "./card-group";
export type { CardGroupProps, CardGroupMode, CardGroupDensity } from "./card-group";
// CollectionContext reste INTERNE (mécanique pattern → Card) : le baril ne l'exporte pas —
// une page n'a jamais à fournir elle-même le contexte d'une collection.
